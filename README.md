# Django Render Service

This app provide template tags to render parts of a template through any render service that implements the GRPC server specified [here](./django_renderservice/render_service.proto).